version: '3'
services:
  db:
    container_name: my_db_9
    ports:
      - "54392:5432"
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_DB=${DB_NAME}
    image: postgres:latest